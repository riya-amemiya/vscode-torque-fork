[package]
name = "torque-compiler"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "V8 Torque frontend compiler for diagnostics and go-to-definition"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
